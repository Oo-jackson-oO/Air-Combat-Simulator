# 作业任务书

# 模拟空战

# 一、基本信息

<table><tr><td>课程名称</td><td>软件体系结构</td></tr><tr><td>作业名称</td><td>模拟空战</td></tr><tr><td>作业类型</td><td>综合实践项目(个人)</td></tr><tr><td>开发语言</td><td>不限</td></tr><tr><td>开发框架</td><td>不限</td></tr><tr><td>AI辅助</td><td>允许使用 AI 工具辅助生成部分代码,但须在报告中明确标注并附带提示词</td></tr><tr><td>代码规模</td><td>追求&quot;最少代码实现最大功能&quot;,结构清晰优先于功能堆砌</td></tr></table>

# 二、训练目标

1. 掌握分层架构的设计思想，能够合理划分 Model / View / Controller（或类似分层），并严格遵守层间依赖约束，大家先尝试，课堂会再讲。 

2. 训练接口先行的软件设计能力：先定义关键组件接口，再借助AI生成实现代码。 

3. 理解性能优化在实际软件中的重要性，以界面帧率（FPS）为核心考核指标，实践 空间索引、碰撞检测、双缓冲等技术。 

4. 运用线程池等并发技术，实现计算与显示的分离，探索"显示层不直接调用模型层" 的约束。 

5. 通过军事题材激发科技报国情怀，了解我国航空装备发展历程与国防安全形势。 

# 三、作业背景与思政融入

# 3.1 背景设定

在信息化战争时代，制空权是决定战争胜负的关键因素之一。近年来，我国自主研发的 歼-20隐身战斗机已列装部队，六代机概念研制也在稳步推进。与此同时，个别国家频繁派 遣舰载机、侦察机在我国周边海域进行抵近侦察和军事挑衅，严重威胁我国领空安全。 

本作业要求模拟一次我方新型战机驱离/拦截敌方挑衅机群的空战场景。学生需设计合 理的软件架构来支撑这一模拟系统的运行。 

# 3.2 阵营设定

<table><tr><td>阵营</td><td>可选机型(示例,自选,参数自设)</td><td>数量设定</td></tr><tr><td>我方(红方)</td><td>歼-20&quot;威龙&quot;隐身战斗机歼-16&quot;潜龙&quot;重型战斗机歼-10C&quot;猛龙&quot;多用途战斗机空警-500预警机(辅助指挥)歼-XX虚拟六代机(学生可自行命名与设定参数)</td><td>1架(玩家操控)</td></tr><tr><td>敌方(蓝方)</td><td>F-22&quot;猛禽&quot;隐身战斗机F-35&quot;闪电II&quot;联合攻击战斗机F/A-18E/F&quot;超级大黄蜂&quot;F-15E&quot;攻击鹰&quot;EA-18G&quot;咆哮者&quot;电子战机</td><td>4~12架(“AI”自动控制,直冲我机,或者随机游走)</td></tr></table>

# 思政元素融入说明：

本作业以国防安全为主题背景，让学生在软件开发实践中潜移默化地了解我国航空装备发展成 就、认识维护国家领空安全的重大意义。选择机型时可引导学生查阅公开资料，了解各型战机 的技术参数和发展历程，增强民族自豪感和科技报国使命感。 

作业中要求设计的"预警机指挥（开视野）"机制，对应现实中的体系化作战理念——现代空战 不是单机对决，而是预警探测、电子对抗、火力打击的体系对抗，这正是软件体系结构中"分层 协作、接口解耦"思想在军事领域的映射。 

# 四、功能要求

# 4.1 核心功能（必须实现）

1. 地图与战场环境：程序运行后显示一片海空域战场地图（俯视图，背景蓝色即可， 或者贴地图网站截图），敌方多架战机从地图边缘侵入我方海域，敌机远的时候看 不到，预警机开视野，我方战机从基地起飞拦截，随着巡逻战斗机开一定距离的视 野，带来计算负荷，同学们也可以自主简化该过程。 

2. 战机移动与操控：我方战机支持键盘（WASD/方向键控制航向和速度）和鼠标 （鼠标指向控制航向，左键发射导弹，右键发射炸弹）两种操作方式，可随时切 换。 

3. 导弹发射与追踪：我方战机可发射空空导弹，导弹具备简易追踪能力（可按比例导 引法或纯追踪法实现）。敌方被导弹命中后从战场消失。 

4. 空中预警与雷达：空警-500为我方提供一定范围的雷达探测能力，在地图上显示敌 方位置标记。预警机可为导弹提供中继制导（加分项）。 

5. 胜负判定：当所有敌方战机被击落或驱离（飞出地图边界）时，显示"胜利"；我方 战机全部被击落时，显示"失败"。界面上实时显示双方剩余战机数量。 

6. 界面刷新率显示：界面左上角或右上角实时显示当前FPS（帧率），以此作为性能 核心指标。 

# 4.2 扩展功能（选做，酌情加分）

- 电子战：敌方电子战机可对我方雷达实施干扰，降低探测范围

- 多编队协同：我方多架战机可组成编队，自动保持编队阵型

- 战损评估与战报生成：空战结束后输出战损统计报告

- 音效反馈：发射导弹、命中、被锁定等事件音效

- 网络对战接口预留：定义好网络通信的消息协议接口（不要求实现，但要预 留）

# 五、架构要求（核心考核点）

# 5.1 分层架构

程序必须采用分层架构实现。推荐但不限于以下经典分层方式： 

<table><tr><td>层次</td><td>职责</td><td>示例组件</td></tr><tr><td>显示层(View)</td><td>负责界面渲染、用户输入捕获、帧率统计显示。仅从控制层获取数据,不得直接调用模型层接口。</td><td>GameRenderer、InputHandler、MiniMap</td></tr><tr><td>控制层(Controller)</td><td>接收用户输入和游戏事件,协调模型层运算与显示层刷新。维护游戏主循环(Game Loop),调度线程池任务。</td><td>GameController、CombatController、AITaskScheduler</td></tr><tr><td>模型层(Model)</td><td>管理游戏状态:战机状态、导弹状态、战场态势、碰撞判定、AI决策等纯数据逻辑,不依赖显示层。</td><td>Battlefield、Fighter、Missile、EarlyWarningAircraft、RadarSystem、AIController</td></tr></table>

# 架构硬约束：

1) 显示层禁止直接new/ 调用模型层对象，必须通过控制层中转。 

2) 模型层禁止引用任何UI 相关类，再思考为什么需要该约束？ 

3) 违反上述约束的代码，架构部分直接扣50%分。 

# 5.2 关键组件与接口设计

学生在编码之前，必须先设计关键组件的接口（interface / 抽象类），回顾面向对象程 序设计的 8原则，经 AI 辅助生成实现代码后，再集成到系统中。以下为必须设计的核心接 口（接口方法可自行增减，但须覆盖所列能力）： 

<table><tr><td>接口名</td><td>参考核心方法</td><td>说明</td></tr><tr><td>IFighter</td><td>Move(), Fire(), TakeDamage(), GetState(), Update()</td><td>所有战机(我方/敌方)的统一抽象。不同机型通过实现类体现参数差异。</td></tr><tr><td>IMissile</td><td>Launch(), Track(), Detonate(), Update()</td><td>导弹的行为抽象。Track()内实现追踪算法。</td></tr><tr><td>IRadarSystem</td><td>Scan(), GetDetectedTargets(), SetRange()</td><td>雷达探测系统。空警-500的雷达范围大于普通战机。</td></tr><tr><td>ICombatJudger</td><td>CheckCollision(), JudgeHit(), UpdateBattleStatus()</td><td>碰撞检测与战斗结果判定。</td></tr><tr><td>IAIController</td><td>Decide(), Execute(), SetDifficulty()</td><td>敌方AI行为决策。可选:简单追踪/编队飞行/逃跑等策略。</td></tr><tr><td>INetworkProtocol(预留)</td><td>Serialize(), Deserialization(), GetMessageType()</td><td>网络对战消息协议接口。仅需定义,不要求实现。</td></tr></table>

# AI 辅助代码生成流程建议：

步骤1：自己画出架构图，定义各层职责和组件清单。 

步骤 2：用代码写出上述接口的定义（interface关键字）。 

步骤 3：将接口定义和设计说明提交给AI，请求生成某个组件的实现代码。 

步骤 4：审查 AI 生成的代码，确认其符合分层约束后再集成。 

步骤 5：在作业报告中标注哪些代码是 AI 生成的，并说明你做了哪些修改和调整。 

# 六、性能要求

# 6.1 核心指标：帧率（FPS）

本作业以程序运行帧率作为第一考核指标。在同等硬件条件下（以教室电脑为基准）， FPS 越高越好。 

<table><tr><td>可能的 FPS 范围</td><td>评级</td><td>说明</td></tr><tr><td>≥ 60 FPS</td><td>优秀</td><td>流畅运行,无明显卡顿</td></tr><tr><td>30 ~ 59 FPS</td><td>良好</td><td>基本流畅,偶有掉帧</td></tr><tr><td>15 ~ 29 FPS</td><td>及格</td><td>可运行,有明显卡顿</td></tr><tr><td>&lt; 15 FPS</td><td>不及格</td><td>严重影响体验</td></tr></table>

# 6.2 性能优化技术要求

学生须在作业报告中阐述以下方面的优化思考（不要求全部实现，但须有分析）： 

<table><tr><td>优化领域</td><td>关键问题</td><td>推荐技术手段</td></tr><tr><td>碰撞检测</td><td>N个物体两两碰撞检测复杂度为<eq>O(N^2)</eq>,当物体增多时性能急剧下降</td><td>空间分区:四叉树(Quadtree)/网格划分(Spatial Hashing);先做 AABB粗检测,再做精确检测</td></tr><tr><td>地图搜索/寻路</td><td>导弹追踪目标和“AI”寻路需要频繁计算</td><td>简化为2D平面距离计算;预计算距离矩阵;使用对象池复用计算结果</td></tr><tr><td>界面刷新</td><td>频繁重绘导致帧率下降</td><td>双缓冲(Double Buffering);脏矩形重绘(仅刷新变化区域);将计算与渲染分离到不同线程</td></tr><tr><td>线程池</td><td>碰撞检测、AI计算等耗时操作阻塞主线程</td><td>使用语言标准库线程池(如Java ExecutorService、C#ThreadPool、Python concurrent.futures);将物理计算提交到后台线程,主线程只负责渲染</td></tr><tr><td>对象池</td><td>频繁new/销毁导弹和爆炸特效导致 GC 压力</td><td>预先创建对象池,导弹发射时从池中取用,爆炸后回收到池中</td></tr></table>

# 6.3 线程池使用要求

程序中必须使用线程池技术。推荐的使用方式： 

- 将碰撞检测任务提交到线程池并行执行，每帧仅等待计算结果

- 敌方 AI决策计算放入线程池，避免阻塞游戏主循环

- 雷达扫描计算可异步执行

线程池的核心作用是确保显示层不被模型层的计算阻塞，从而维持高帧率。这与分层架 构中"显示层不直接调用模型层"的约束相辅相成。 

# 七、界面要求

# 7.1 视觉呈现

界面元素的显示允许使用以下两种方式（任选其一或混用），不要求也不鼓励使用外部 图片资源： 

- 几何图形组合：用矩形、三角形、圆形、线条等基本图形组合绘制战机、导 弹、雷达扫描线等。 

- 图标贴图：可使用小尺寸PNG图标（如32×32或64×64）作为战机标识。 

战机的外形还原度不是评分重点。推荐做法：用不同颜色的三角形/箭头区分敌我，用 大小表示机型，用虚线圆表示雷达探测范围。 

# 7.2 信息显示

界面上应至少显示以下信息： 

- 战场地图（含海域/空域背景） 

- 我方与敌方战机的位置和航向 

- 雷达探测范围（可选：扇形/圆形扫描动画） 

- 实时 FPS 

- 双方剩余战机数量 

- 当前操控战机的状态（速度、剩余导弹数、生命值） 


# 7.3 操作方式

<table><tr><td>输入设备</td><td>操作映射</td></tr><tr><td>键盘</td><td>W/S或上/下:加速/减速;A/D或左/右:左转/右转;空格:发射导弹;Tab:切换操控战机</td></tr><tr><td>鼠标</td><td>鼠标移动:控制战机航向(指向哪里飞向哪里);左键点击:发射导弹;右键点击:选择目标;滚轮:缩放地图(选做)</td></tr><tr><td>同时支持</td><td>两种操作方式应能随时切换,不重启程序;用户在一种输入方式操作时,另一种方式的输入不应干扰</td></tr></table>

# 八、网络对战接口预留

程序须预留网络对战接口，但不要求实际实现网络通信功能。具体要求： 

1. 定义INetworkProtocol 接口，包含消息序列化、反序列化和消息类型标识方法。仔 细考虑序列化为二进制流还是 json字串？ 

2. 定义至少以下消息类型（枚举或常量）： 

    - MSG_FIGHTER_MOVE：战机位置/状态同步 
    - MSG_MISSILE_LAUNCH：导弹发射通知 
    - MSG_HIT_RESULT：命中结果通知
    - MSG_BATTLE_STATUS：战场态势同步 

3. 在控制层预留网络消息的发送/接收入口方法（方法体可为空，但须有注释说明预 期行为）。 

4. 在报告中说明网络对战时的预期架构调整方案（简要即可）。 

# 九、代码质量要求

1. 代码量精简：在满足功能的前提下追求最少代码量，避免冗余代码和过度设计。 

2. 结构清晰：包/命名空间划分合理，类名和文件名见名知义，各层代码严格分离。 

3. 注释适度：接口和关键算法处有注释说明设计意图；AI生成的代码段须有标注。 

4. 可编译运行：提交的源码须能直接编译运行，不依赖特殊环境配置（如需数据中间 件等须在报告中说明替代方案）。 

# 十、交付物要求

<table><tr><td>序号</td><td>交付物</td><td>格式要求</td><td>说明</td></tr><tr><td>1</td><td>作业报告</td><td>PDF(推荐)或Word只要电子版不要打印</td><td>报告应简洁(建议5~10页),须包含以下内容:程序架构图(分层结构、组件关系)关键组件说明及接口定义(代码片段或UML图均可)组件之间的调用关系与数据流说明性能优化分析(碰撞检测、线程池、双缓冲等采用了哪些优化,效果如何)AI辅助说明(哪些代码由AI生成,做了哪些修改,附录AI提示词及效果)网络对战接口设计说明(简要)运行截图(至少2张:战场全景、FPS显示)</td></tr><tr><td>2</td><td>演示视频</td><td>MP4格式,时长&lt;5分钟</td><td>视频内容应包含:程序启动与战场初始化(展示敌机入侵和我方拦截过程)键盘操作演示(至少30秒)鼠标操作演示(至少30秒)导弹发射与命中效果FPS显示(展示性能)</td></tr><tr><td>3</td><td>程序源码以及可执行程序</td><td>压缩包(zip / rar)</td><td>须包含:全部源代码(含项目配置文件)README文件:说明编译运行环境、启动方式、操作说明若使用AI生成代码,在对应文件头部用注释标注可执行程序,最好是单文件exe可执行程序,如果有dll请使用zip格式将exe和dll封装到一个目录;打包为安装程序也可以;</td></tr></table>

# 十一、评分标准

<table><tr><td>序号</td><td>评分项</td><td>权重</td><td>评分要点</td></tr><tr><td>1</td><td>分层架构合理性</td><td>30%</td><td>层次划分是否清晰;层间依赖方向是否正确;显示层是否未直接调用模型层;代码组织是否体现分层思想</td></tr><tr><td>2</td><td>性能(FPS)</td><td>20%</td><td>同硬件条件下的帧率表现;是否使用了线程池、空间索引等优化技术</td></tr><tr><td>3</td><td>接口设计与组件设计</td><td>15%</td><td>接口定义是否完整合理;组件职责是否单一;是否遵循"接口先行"流程</td></tr><tr><td>4</td><td>功能完整性</td><td>15%</td><td>核心功能是否全部实现;操控是否流畅;胜负判定是否正确;鼠标和键盘是否同时支持</td></tr><tr><td>5</td><td>作业报告质量</td><td>10%</td><td>架构图是否清晰;优化分析是否有深度;AI使用是否如实标注;报告是否简洁</td></tr><tr><td>6</td><td>代码质量</td><td>10%</td><td>代码是否精简;命名是否规范;是否可编译运行;网络接口是否预留</td></tr></table>

> 加分项（最多+5分）：
>
> - 预警机中继制导功能实现（+2分） 
>
> - 敌方AI 具备编队飞行或战术回避行为（+2分） 
>
> - 代码总量精简且结构优雅（评审组酌情+1分） 
>

# 十二、进度安排（建议）

<table><tr><td>阶段</td><td>时间</td><td>任务</td></tr><tr><td>架构设计</td><td>第1周</td><td>阅读任务书,设计分层架构图,定义所有核心接口,撰写接口文档</td></tr><tr><td>核心开发</td><td>第2~3周</td><td>实现模型层(战机、导弹、碰撞检测)、控制层(游戏主循环、线程池调度)</td></tr><tr><td>显示与交互</td><td>第3~4周</td><td>实现显示层渲染、键盘和鼠标双操控、FPS显示</td></tr><tr><td>优化与测试</td><td>第4~5周</td><td>性能优化(碰撞检测、双缓冲、对象池等),功能测试,录制演示视频</td></tr><tr><td>报告与提交</td><td>第5~6周</td><td>撰写作业报告,整理源码,提交全部交付物</td></tr></table>

# 十三、补充说明

1. 关于 AI 辅助：鼓励合理使用 AI工具，但学生必须理解AI 生成代码的逻辑。作业 报告中须明确标注 AI 生成部分，并在答辩（如有）时能够解释相关代码。完全复 制粘贴AI 代码且不加理解的行为视为学术不端。 

2. 关于代码规模：优秀的分层架构不需要大量代码。评分时代码量不是加分项，精简 才是，代码越少越好。 

3. 关于语言和框架：选择自己最熟悉的语言和框架即可。 

4. 关于运行环境：提交的代码须在普通Windows电脑上可直接编译运行（无需GPU 加速，纯CPU软件渲染即可）。 
